# 核心依赖
websockets>=12.0            # WebSocket 客户端（数据源）
aiohttp>=3.9                # HTTP 客户端 + Web 服务
aiohttp-socks>=0.8          # SOCKS5 代理支持
python-binance>=1.0.36      # Binance API SDK

# 桌面端依赖
customtkinter>=5.2.2        # 原生 Mac 桌面 UI
